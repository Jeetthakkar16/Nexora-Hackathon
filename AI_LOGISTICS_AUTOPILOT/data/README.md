# Data

No fake operational dataset is bundled.

ETA CSV columns:
distance_km,hour,day_of_week,weather_index,traffic_index,eta_minutes

DELAY CSV columns:
distance_km,hour,day_of_week,weather_index,traffic_index,delay
